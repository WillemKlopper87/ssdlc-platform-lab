# Single target: the same Docker host this repo's compose/ profiles run
# on. No remote hosts yet -- see docs/adr/0010's scope note on why "hosts"
# in DESIGN.md's terraform/ description (real cloud VM provisioning) is
# not attempted yet, and the same reasoning applies here: this inventory
# only needs to grow once there is a real remote target to test against.

[ssdlc_platform]
localhost ansible_connection=local
